galaxy_info:
  author: coaxial
  namespace: coaxial
  description: Install Transmission and optionally configure nginx
  role_name: transmission

  license: MIT

  min_ansible_version: 5.6.0

  platforms:
    - name: ubuntu
      versions:
        - focal
        - bionic

  galaxy_tags: [transmission, servarr]

dependencies: [nginxinc.nginx]
